{
  "proxy_key": "proxy-{{ .Values.data.name }}",
  "zone_key": "{{ .Values.global.zone}}",
  "name": "{{ .Values.data.name }}",
  "domain_keys": [
    "domain-{{ .Values.data.name }}",
    "domain-{{ .Values.data.name }}-egress"
  ],
  "listener_keys": [
    "listener-{{ .Values.data.name }}",
    "listener-{{ .Values.data.name }}-egress"
  ],
  "listeners": null
}
